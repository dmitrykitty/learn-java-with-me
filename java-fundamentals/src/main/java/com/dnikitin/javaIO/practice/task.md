Here are the translations of the tasks into English:

1.  **Given a text file, find and print to the console all words starting with a vowel.**
2.  **Given a text file, find and print to the console all words where the last letter of one word matches the first letter of the next word.**
3.  **Given a text file. For each line, find and print the maximum number of consecutive digits.**
4.  **Given a file with Java code. Read the program text from the file and replace the word `public` with `private` 
in the declarations of class attributes and methods. Save the result to another pre-created file.**
5.  **Given a file with Java code. Read the program text from the file and write the characters of each line in reverse order to another file.**